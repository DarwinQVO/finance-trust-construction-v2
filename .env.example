# Environment Variables for Finance Trust Construction v2.0
# Phase 4: Deployment
# Date: 2025-11-06
#
# Copy this file to .env and fill in actual values
# DO NOT commit .env to version control

# OpenAI API Key (optional, for LLM-based merchant detection)
# If not set, will use pattern-matching only
OPENAI_API_KEY=sk-...

# Log Level (debug, info, warn, error)
LOG_LEVEL=info

# Datomic URI (set automatically by Docker Compose)
DATOMIC_URI=datomic:free://datomic:4334/finance

# ML Service URL (set automatically by Docker Compose)
ML_SERVICE_URL=http://python-ml:8000
